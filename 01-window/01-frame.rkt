#lang racket/base
;; =========================================================
;; 01-window/01-frame.rkt —— 第一步：开一个窗口
;; 运行：racket 01-window/01-frame.rkt
;; 关闭：点右上角 X（本步点了只是把窗口藏起来，进程没退出——下一步解决）
;; =========================================================
;; 本步新增（2 个）：
;;   frame% —— 窗口类（标题栏 + 内容区）
;;   show   —— 显示窗口的方法
;;
;; ★核心心智：racket/gui 是"事件驱动"的，没有你手写的 while 主循环。
;;   你创建窗口、把它 show 出来，然后把控制权交给系统——系统在"有事发生"
;;   时（点关闭、拖大小、需要重画）回调你写好的处理方法。
;;   这个模型贯穿后面每一课。
;; =========================================================

(require racket/gui)   ; frame% canvas% timer% 等窗口类都在这里

;; 造一个窗口实例。label=标题栏文字；width/height=内容区大小（像素）。
;; 刚创建时窗口还不可见，必须 show 一下才会出现。
(define frame (new frame% (label "01-01 窗口") (width 400) (height 300)))

;; 显示窗口。#t 让它显示出来。
(send frame show #t)
